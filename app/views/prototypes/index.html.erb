<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        <%= "こんにちは、"%><%= link_to @user.name, user_path(current_user.id), class: :greeting__link %><%= "さん"%>
        <%# <%= link_to "ユーザー名さん", root_path, class: :greeting__link%>
      </div>
    <%# // ログインしているときは上記を表示する %>
    <% end %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
      <%= render @prototypes %>
    </div>
  </div>
</main>
